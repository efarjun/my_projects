# Retrieves all available site to site VPN connection IDs:

#!/usr/bin/python3

import boto3
client= boto3.client('ec2')
vpns = client.describe_vpn_connections(
    Filters=[
        {
            'Name': 'state',
            'Values': [
                'available',
            ]
        },
    ],
)

for vpnId in vpns['VpnConnections']:
    print(vpnId['VpnConnectionId'])
